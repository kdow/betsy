<section class="grid-container">

  <%= render "shared/dashboard_header", locals: { seller: @current_seller } %>
  <section class="grid-body">
    <h2>Edit <%= @product.name %></h2>

    <%= render "form", url: seller_product_path(session[:seller_id], @product)   %>
  </section>
</section>